package org.skr.registry.repository;

import org.skr.registry.model.SiteUrl;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SiteUrlRepository extends JpaRepository<SiteUrl, String> {

}
